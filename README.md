# agentic-task-executor
An Agentic AI system that takes a high-level task, decomposes it into steps, executes them iteratively, maintains memory, and produces a final result — built from scratch without frameworks.
